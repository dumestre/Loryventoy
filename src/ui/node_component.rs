// Thin re-export wrapper — all inspector logic lives in `inspector/`.
pub use crate::ui::inspector::{
    AcaoInspector, CABECALHO_H, FONTE_TITULO, MARGEM_Y,
    content_size, linha_y,
    render_layer_header, render_layer_row, show_content,
};
