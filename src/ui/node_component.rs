// Thin re-export wrapper — all inspector logic lives in `inspector/`.
pub use crate::ui::inspector::{
    content_size, linha_y, render_layer_header, render_layer_row, show_content, AcaoInspector,
    CABECALHO_H, FONTE_TITULO, MARGEM_Y,
};
